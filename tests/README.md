# Test Scaffold

Phase 1 only adds test directories and placeholder coverage targets.

- `tests/ton-storage/`: parsers, validators, command builder, and service tests
- `tests/ssh/`: SSH client, exec, and SFTP transport tests
- `tests/api/`: route handler and API contract tests
