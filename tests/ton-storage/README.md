# TON Storage Tests

Planned coverage:

- command builder argument rules
- parser fixtures for verified `storage-daemon-cli` output
- validator behavior for bag IDs and future request payloads
- service-layer orchestration with mocked transport
